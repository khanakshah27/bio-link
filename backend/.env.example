# Copy to .env and fill in real values.

# PostgreSQL connection string
DATABASE_URL=postgresql+psycopg2://biolink:biolink@localhost:5432/biolink

# NER model names (must already be `python -m spacy download <name>`'d,
# or, for scispacy, pip-installed per the README)
SCISPACY_MODEL=en_core_sci_sm
SPACY_FALLBACK_MODEL=en_core_web_sm

# Optional: raises your NCBI E-utilities rate limit from 3 to 10 req/sec
NCBI_API_KEY=

# Optional, but powers three features when set:
#   - an abstractive (Gemini-written) paper summary instead of the
#     built-in extractive summarizer
#   - LLM-based relationship extraction (sentence + entities -> relation
#     type), instead of the fixed-verb-list regex extractor
#   - "Ask Bio-Link", the Q&A chat over each paper's extracted knowledge
# Leave blank to fall back to the offline extractive summarizer and the
# regex relationship extractor; Ask Bio-Link will report that it needs a
# key configured. Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Keep "true" so the UI still shows example data for a few well-known
# genes/diseases when an external database is unreachable (offline demo,
# restricted network, brief outage). Set "false" to surface raw errors.
USE_OFFLINE_FALLBACK=true

REQUEST_TIMEOUT_SECONDS=8
MAX_UPLOAD_MB=20
CORS_ORIGINS=*
